<p>
  <strong>Num:</strong>
  <%= @course.no %>
</p>
<p>
  <strong>Title:</strong>
  <%= @course.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @course.text %>
</p>

<h2>Wants</h2>
<%= render @course.wants %>

<h2>Add a want:</h2>
<%= render "wants/form" %>

<%= link_to 'Back', courses_path %>
| <%= link_to 'Edit', edit_course_path(@course) %>
